<div class="container">
<h1 align="center">ТЕСТ ЗАВЕРШЕНО</h1>

<!--<%= @balu %>
<%= @record.user.nickname %><br>-->
<div class="progress">
  <div class="progress-bar bg-success" role="progressbar" style="width: <%= (@record.score * 100) / 20 %>%" aria-valuenow="15" aria-valuemin="0" aria-valuemax="100"><%= (@record.score * 100) / 12 %>%</div>
  <div class="progress-bar bg-danger" role="progressbar" style="width: <%= 100 - (@record.score * 100) / 12 %>%" aria-valuenow="30" aria-valuemin="0" aria-valuemax="100"></div>
</div>
	
	<h2>Ваш результат: <%= @record.score %> правильні відповіді із 12</h2>
	<hr>
<div class="row">
	<div class="col-md-4">
	</div>
	<div class="col-md-3">
	
	<% @max_result = Result.where(user_id: current_user.id).order(created_at: :DESC).map.max{|rec| rec.score}%>
	Каристувач: <%= @max_result.user.nickname%><br><hr>
	Приєднався: <%= current_user.created_at.to_date.strftime("%d %B, %Y") %><br>
	Максимальний результат: <%= @max_result.score%><br>
	Рекорд установлено: <%= @max_result.created_at.to_date.strftime("%d %B, %Y")%>
	</div>
	<div class="col-md-5">
	</div>
</div>
</div>
